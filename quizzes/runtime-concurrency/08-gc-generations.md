# 08. GC Generations — Quiz

상태: **답변 대기**

1. .NET GC가 Generation을 나누는 이유는 무엇인가?
2. Gen 0 / Gen 1 / Gen 2의 차이를 설명하라.
3. 객체가 상위 Generation으로 승격되는 것은 무엇을 의미하는가?
4. GC Root란 무엇인가?
5. Reachable object와 unreachable object의 차이는?
6. GC가 있어도 Managed Memory Leak이 발생할 수 있는 이유는?
7. Large Object Heap이 일반 객체와 다른 이유는?
8. 큰 `byte[]`를 반복 생성하는 Backend 작업이 어떤 문제를 만들 수 있는가?
9. TIFF→PDF MemoryStream 최적화가 memory pressure와 어떤 trade-off를 만드는가?
10. Allocation Rate와 Heap Size를 함께 봐야 하는 이유는?
11. Stop-the-world pause를 과도하게 단순화하면 안 되는 이유는?
12. 60초 안에 .NET GC의 Generation 구조와 Backend 성능 영향을 설명하라.

## 평가 기준

- 세대별 GC 목적을 설명한다.
- Reachability와 GC Root를 이해한다.
- GC != memory leak 없음 을 설명한다.
- Allocation/LOH/latency를 Backend 관점으로 연결한다.

## 평가 결과

- 점수: Pending
- 보완할 개념: Pending
- 재시험: Pending
